# The API-usage surface: the sidebar-foot balance/usage panel plus the General
# settings toggle. Installed as an out-of-tree profile bundle via
# `dsh plugin --profile <name> add dsh-api-usage`.
- insert:
    - id: api-usage
      name: 'dsh-api-usage'
